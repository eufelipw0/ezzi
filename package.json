{
  "name": "@ezzi/root",
  "version": "1.0.1",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "bun run --filter=bot dev",
    "build": "bun run --filter=\"./packages/*\" build",
    "check": "bun run --filter=\"./packages/*\" check",
    "test": "bun run --filter=\"./packages/*\" test\""
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.3"
  }
}
